/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com', 'localhost', '127.0.0.1', 'mymedicserver.pythonanywhere.com']
    },
  };
  
  export default nextConfig;
  